export default function handler(req, res) {
  res.status(200).json({
    id: req.query.codigo,
    id: 2105,
    nome: `Dennys ${codigo}`,
    idade: 32,
    email: `dennys.projetos@outlook.com`
  })
}